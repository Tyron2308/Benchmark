from enum import Enum
class Option(Enum):
    TOPIC = "topic"
    NBRECORD = "num-record"
    THROUGHPUT = "throughput"
    CONFIG = "config-path"
    RECORDSIZE = "record-size"
    PAYLOADFILE = "payload-file-path"

